# Day 1 运维笔记（人话版）

> 日期：2026-09-05 ｜ 设备：MacBook Air ｜ 服务器：运维练手一号（腾讯云轻量应用服务器）
> 用途：抄进你自己的"运维学习备忘录"，每条都配了"是啥 + 能干嘛"。

---

## 一、今天我干了什么（按顺序）

1. 在腾讯云申请了一台**轻量应用服务器**（2核2G、3M 带宽、北京机房、**1 个月免费**，2026-10-05 到期）
2. 把实例名改成「**运维练手一号**」
3. 用网页终端 **OrcaTerm**，以 **root（超级管理员）** 身份登进了服务器
4. 敲了第一批命令，认识这台机器
5. 敲了第二批命令，练习建文件 / 文件夹
6. 踩了一个坑：把教程里的解释文字一起粘进终端，产生了两个"脏文件"，用 find 命令清理掉了（**教训：只复制代码块里的命令**）
7. 敲了第三批命令：往文件写内容、读内容、复制、删除、追加
8. 用 passwd 改了 root 密码（这本来是 Day 2 的事，提前做了）

---

## 二、每个命令是啥、能干嘛（人话版）

### 认识机器类
| 命令 | 是啥 | 能干嘛 |
|---|---|---|
| `whoami` | 我是谁 | 运维同时管好几台服务器，敲命令前先确认**没登错机器** |
| `pwd` | 我在哪个文件夹 | 防止在错的目录删错东西（很多线上事故就这么来的） |
| `uname -a` | 内核身份证号 | 看这台机器的内核版本 |
| `cat /etc/os-release` | 系统自我介绍 | 我的是 **OpenCloudOS 9.6**。装软件前必须先知道系统，因为不同系统装软件的命令不一样（OpenCloudOS 用 dnf，Ubuntu 用 apt） |

### 看东西类
| 命令 | 是啥 | 能干嘛 |
|---|---|---|
| `ls -la` | 列出当前文件夹所有东西（含隐藏文件） | 排查问题第一步永远是"先看看里面有啥" |
| `cat 文件名` | 把文件内容打印到屏幕 | 以后看日志、看配置全靠它 |

### 建东西类
| 命令 | 是啥 | 能干嘛 |
|---|---|---|
| `mkdir test` | 建个文件夹 | 部署网站时建目录放代码 |
| `touch hello.txt` | 建个空文件 | 占位、建日志文件 |

### 写东西类（重点区分）
| 命令 | 是啥 | 能干嘛 |
|---|---|---|
| `echo "话" > 文件` | 把话写进文件，**覆盖**原内容 | 改配置文件 |
| `echo "话" >> 文件` | 把话**追加**到文件末尾 | 写日志 |

### 复制删除类
| 命令 | 是啥 | 能干嘛 |
|---|---|---|
| `cp a b` | 把 a 复制一份叫 b | **运维保命习惯：改配置前先备份，改坏了能恢复** |
| `rm 文件` | 删除，**不可恢复** | 清理空间，敲之前想清楚 |

### 顺手工具类
| 操作 | 是啥 |
|---|---|
| `find …` | 按条件找文件（今天用它清了脏文件） |
| `passwd` | 改密码，服务器安全第一件事 |
| `clear` | 清屏 |
| `Ctrl+C` | 中断卡住的命令 |
| `↑` | 调出上一条命令 |
| `Tab` | 自动补全 / 提示命令 |

---

## 三、这些串起来能干嘛（回答"有啥用"）

想象以后你的服务器上跑着网站，**半夜网站挂了**，你的救火流程：

1. ssh 登上去 → `whoami` / `pwd` 确认没登错机器
2. `ls` / `cat` 翻日志文件找报错
3. `cp` 备份配置 → 改配置
4. 重启服务 → 网站恢复

**Day 1 学的每个命令，都是这条救火路线上的一环。运维的日常工作 = 这些命令的组合。** 单个看没用，合起来就是你以后上班天天干的事。

---

## 四、我现在的资产

- 一台真实跑在公网上的服务器：**运维练手一号（140.143.127.69）**
- 它能干的事：挂我未来的校内二手平台、跑监控告警脚本、当 Linux 练习场
- 简历上已经能写的一句：**"独立申请并运维一台云服务器（OpenCloudOS），完成基础环境配置与文件管理"**

---

## 五、Day 2 预告（明天或后天做，不急）

- 创建新用户 `langyu`，给它管理员权限（以后不用 root 日常登录，更安全）
- `dnf update -y` 给系统打补丁（把安全漏洞的门关紧）
