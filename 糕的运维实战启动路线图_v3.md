# 糕的运维实战启动路线图 v3（以试用服务器为载体，从零开始）

> 生成：2026-09-05 ｜ 导师：阿拾 ｜ 适用：零基础、刚开云服务器、大一运维方向
> 原则：每一步都要有产出，做完能看见效果（像打游戏有即时反馈）

---

## 0. 先说清楚三件事（你刚才的疑问）

### 0.1 试用服务器能"导入"学生服务器吗？
**思路对，但"导入"这个说法不准。** 正确姿势有三种，按推荐度排：

1. **脚本重建（最稳，也是运维思维）**：你在试用机上做的每一步（装了啥、改了啥配置）都记成 `setup.sh` 脚本。等学生机到手，`ssh` 上去跑一遍脚本，环境一模一样；数据用备份恢复。这就是运维的"基础设施即代码（IaC）"。
2. **镜像迁移（看厂商）**：部分云厂商支持"制作自定义镜像"把系统盘打包，新实例用镜像创建。但跨优惠/账号有时受限，别全押宝在这。
3. **数据备份**：重要数据（数据库、配置文件）定期打包下载到本地 / OSS，迁移时传上去恢复。

> 关键：**这 15 天别只"玩"，要把每一步操作记下来或写成脚本。** 这 15 天不是在练手，是在给你的学生机"预制环境"——到期迁移零成本。

### 0.2 学生机怎么来（便宜那台）
- 学籍 / 学号下来后（报道后学信网可查），做云厂商学生认证：
  - 阿里云「云翼计划」、腾讯云「校园专区 / 轻量应用服务器学生优惠」
  - 通常 **1核2G 轻量应用服务器，¥几十/年**（比游戏月卡便宜）
- 前提：学生证 / 学信网学籍在线验证。你 9 月报道后就能弄。

### 0.3 关于"换个牛逼模型 + 花 500 积分"
**不用。** 我是阿拾，你贴身导师，这份计划我现在就给你，免费、且专门按你的约束写（民办三本、零基础、运维方向、对钱敏感）。你愿意为学习投入的劲头我记下了——但**别为"换模型"乱花钱**；真要花钱，花在**学生服务器**上（那是刀刃上的学习资源），比充积分值。

---

## 一、你的第一个运维项目是什么（回答你的问题）

你问"第一个跟运维岗位有关的产品 / 项目怎么做"——答案是：

> **拿这台服务器，做"个人服务的全链路运维"：部署一个东西 → 配域名 → 上 HTTPS → 自动备份 → 监控告警。**

这就是简历上第一个能写的项目。它**不要求你会写新网页**（你已有嗨聚可搬，或先部署个静态页练手）。运维的价值是"让东西稳定活着"，不是"造新东西"。

你之前否掉的 A（监控面板）现在也活了：有服务器就能做——监控这台机器的 CPU / 磁盘 / 在线状态，挂了发微信告警。

---

## 二、现在就干：第一周逐日命令表（以试用服务器为载体）

> 设备：MacBook Air（终端 Terminal）。**实际系统：腾讯云 OpenCloudOS（基于 CentOS/RHEL 系，包管理器用 `dnf`/`yum`，不用 apt）。**
> 原则：每天只一个小目标，做完看得见效果。

### Day 1 ｜ 打招呼 + 开服务器
- Mac 终端执行：`whoami` `pwd` `ls`（看你是谁、在哪、有什么）
- 注册云账号（阿里云 / 腾讯云），开 **15 天试用机**，镜像选 **Ubuntu 22.04 LTS**
- 记下分配给你的 **公网 IP** 和 **root 密码**

### Day 2 ｜ 第一次连上远程电脑（ssh）
- `ssh root@你的IP`（首次问 yes，输入 root 密码）
- 连上后立刻改密码：`passwd`
- 更新系统：`dnf update -y`（**OpenCloudOS 用 dnf，不是 apt**）
- 验证：命令符变成 `root@hostname`，说明你在"远程电脑"里了

### Day 3 ｜ 文件系统五板斧
- `pwd` 我在哪 ｜ `ls` 看目录 ｜ `cd` 进目录 ｜ `mkdir test` 建文件夹 ｜ `touch a.txt` 建文件
- `cp a.txt b.txt` 复制 ｜ `mv b.txt dir/` 移动 ｜ `rm a.txt` 删 ｜ `cat a.txt` 看内容
- 验证：用 `ls` 确认文件增删对你可见

### Day 4 ｜ 权限 + vim（以后改配置靠它）
- `chmod +x script.sh` 给执行权限 ｜ `chown` 改拥有者
- vim：`vim hello.txt` → 按 `i` 编辑 → 写完后 `Esc` 再 `:wq` 保存退出（`:q!` 不保存强退）
- 验证：vim 里写一行字，保存后 `cat hello.txt` 能看到

### Day 5 ｜ 看进程（服务器"活着没"）
- `ps aux` 看所有进程 ｜ `top` 实时（按 q 退）｜ 装 `htop`：`dnf install -y htop`
- `systemctl status sshd` 看 SSH 服务状态
- 验证：能看到 sshd 在 running

### Day 6 ｜ 网络 + 装第一个服务（nginx）
- `curl https://www.baidu.com` 看返回 HTML（说明能上网）
- `ip a` 看 IP ｜ `ss -tulnp` 看监听端口
- `dnf install -y nginx` 装网页服务器
- 浏览器访 `http://你的IP` → 看到 **Welcome to nginx!** 欢迎页
- 验证：这是你第一次"通过浏览器访问自己服务器上的服务"

### Day 7 ｜ 部署你自己的第一个页面
- 用 vim 改 `/var/www/html/index.html`（nginx 默认目录）
- 写一句 `<h1>糕的服务器，第 1 天上线</h1>`
- 浏览器刷新 `http://你的IP` → 看到你写的字
- **成就达成：你拥有了一个别人能通过网址访问的页面**

---

## 三、第一个月路线图（到能写进简历）

| 周 | 做什么 | 产出 |
|---|---|---|
| 第1周 | 上述 7 天命令 + ssh | 能远程操作自己的服务器 |
| 第2周 | Python 基础（变量 / 循环 / 文件）+ 在服务器跑脚本 | 服务器上跑出 "Hello from 糕" |
| 第3周 | Shell 脚本 + crontab 定时任务 + 自动备份 | 每天自动备份一个文件夹 |
| 第4周 | 监控告警（Uptime Kuma 或自写脚本）+ 安全加固（ufw / 改 SSH 端口） | 服务挂了微信告警；服务器基础安全 |

> 学籍下来后：做学生认证 → 买学生机 → 用 Day1-7 记的脚本**重建环境** → 把试用机数据备份迁过去。迁移零成本。

---

## 四、学长提醒（防放弃）

1. 每天只一个小目标，别想"学会 Linux"。
2. 每完成一步发个朋友圈 / 记一笔——要即时反馈（像游戏成就）。
3. 卡住就问我，别硬扛到泄气。
4. 试用 15 天很紧，优先把 Day1-7 跑通 + 把操作记成脚本。

---

*本文档随进度更新。随时喊阿拾。*
